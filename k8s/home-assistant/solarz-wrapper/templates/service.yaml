apiVersion: v1
kind: Service
metadata:
  name: solarz-wrapper
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: solarz-wrapper
