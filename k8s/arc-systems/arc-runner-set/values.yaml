gha-runner-scale-set:
  ## URL do repositório ou organização
  githubConfigUrl: "https://github.com/catdevsecops/home-automated-infrastructure"

  ## Nome da Secret gerada pelo seu ExternalSecret
  githubConfigSecret: "github-app-secret"

  ## Configuração do Runner
  minRunners: 1
  maxRunners: 3

  ## Se você for usar Docker dentro dos runners (build de imagens)
  containerMode:
    type: "dind"

  dindArgs:
    - "--mtu=1300"

  ## Configuração do Controller
  controllerServiceAccount:
    name: arc-systems-arc-operator-gha-rs-controller
    namespace: arc-systems

  ## Nome do Runner Group no GitHub (opcional)
  runnerScaleSetName: "k8s-runner-set"

  listenerTemplate:
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: listener
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
